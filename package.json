{
  "name": "claude-code-vercel-proxy",
  "version": "2.0.0",
  "description": "Anthropic API proxy via Vercel AI Gateway for Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^1.0.11",
    "ai": "^5.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.0.0"
  }
}
