{
  "name": "claude-code-vercel-proxy",
  "version": "2.3.0",
  "description": "Cloudflare Worker proxy for Anthropic API via Vercel AI Gateway",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@ai-sdk/gateway": "^1.0.11",
    "ai": "^5.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.0.0"
  }
}
